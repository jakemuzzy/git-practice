# practice repo learning git


## commands used
- git init
- git status
- git commit
- git config
- one more commit to master
- more changes to the merge deme branch 
- another commit to master after first merge!!
- another commit to master after first merge??
- bit branch
- git branch c- name
- adding a change to commit to master
- commit to second branch git checkout -b second-branch

- log and diff stuff 

- fetching a clean file; and some stuff to pull

- adding some stuff about stash

## doing another commit
